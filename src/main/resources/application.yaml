spring:
  application:
    name: employees-service


  datasource:
    url: jdbc:mysql://localhost:3307/invex_db?serverTimezone=UTC
    #url: jdbc:mysql://mysql8:3307/msvc_usuarios?serverTimezone=UTC
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true


management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,mappings
  endpoint:
    health:
      show-details: always
      #show-details: never

logging:
  level:
    com.invex.employees: INFO